import { ClasseIndicatoreNonCe } from './classe-indicatore-non-ce';

describe('ClasseIndicatoreNonCe', () => {
  it('should create an instance', () => {
    expect(new ClasseIndicatoreNonCe()).toBeTruthy();
  });
});
