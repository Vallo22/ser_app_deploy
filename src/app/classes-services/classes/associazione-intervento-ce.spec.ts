import { AssociazioneInterventoCe } from './associazione-intervento-ce';

describe('AssociazioneInterventoCe', () => {
  it('should create an instance', () => {
    expect(new AssociazioneInterventoCe()).toBeTruthy();
  });
});
