import { CaratteristicheStrutturaSt } from './caratteristiche-struttura-st';

describe('CaratteristicheStrutturaSt', () => {
  it('should create an instance', () => {
    expect(new CaratteristicheStrutturaSt()).toBeTruthy();
  });
});
