import { ClasseIndicatoreCe } from './classe-indicatore-ce';

describe('ClasseIndicatoreCe', () => {
  it('should create an instance', () => {
    expect(new ClasseIndicatoreCe()).toBeTruthy();
  });
});
